## Iteration II Plan

Based on Iteration I evaluation, the main bottleneck is keyword-based retrieval and gating sensitivity.

Iteration II introduces **embedding-based semantic retrieval** to improve semantic matching and reduce gating false negatives.

*The same evaluation dataset and metrics will be used to compare keyword retrieval vs embedding retrieval.*